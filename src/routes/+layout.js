// Prerender the entire site to static HTML at build time.
// Crawlers (Google, social scrapers) get the full page markup without
// executing JS — better SEO + instant first paint on any static host.
export const prerender = true;
