# VanguardOS landing — iteration run · Jun 2026

Scheduled autonomous run. Focus: turn the 5-day day-cards into real **visual proof** without waiting on the missing `day-N.png` assets (pure-CSS artifact previews), rotate the **CTA lever** (pill pool was exhausted last run), and bring the scrollytelling rail to **mobile**. Build verified green.

---

## 1. Files changed

### `src/routes/+page.svelte`
- **Day-card fallback → per-day CSS artifact previews (`#five-day`, ~line 778):** replaced the generic fallback tile (big day number + kicker only) with a distinct pure-CSS mini-mockup of each day's actual deliverable, plus a small day-number/kicker badge pinned top-left. Day 01 IDEATE → validated-idea checklist with `$129 · validated` tag; Day 02 BRAND → monogram + 5-swatch palette + type lines; Day 03 ASSETS → 2×2 grid of asset tiles; Day 04 LANDING PAGE → wireframe (nav, headline blocks, gold CTA, content row); Day 05 FUNNEL → descending funnel bars + `Checkout ✓` chip. Before: every card showed the same number-only placeholder. After: each card reads as a screenshot of the thing it produces.
- **Hero primary CTA (~line 539):** `Build My System · $129 →` → `Start in 5 Days · $129 →`. Rotated the CTA lever per the SKILL.md test list (concrete-outcome variant).
- **Mobile sticky CTA (~line 1501):** `Build My System →` → `Start in 5 Days →` — kept consistent with the hero so the rotation is a clean single-variant test, not a split message.
- **5-day scrollytelling rail (~line 762):** `class="five-day-rail hidden lg:block"` → `class="five-day-rail block"`. The scroll-linked gold rail + 5 sequentially-lighting marker dots now render on mobile too, where they read as a standalone 5-step progress indicator above the stacked cards. Comment updated to match.

### `runs/2026-06-10.md`
- This log (new).

### Housekeeping
- Removed the live stale `vite.config.js.timestamp-*.mjs` scratch files. The `.svelte-kit-old-*` / `.svelte-kit-stale-*` / `.svelte-kit.bak*` directories are **locked by the mounted filesystem** (EPERM on unlink) and could not be deleted this run — flagged in §5. They do not affect the build; a fresh `.svelte-kit` builds clean.

---

## 2. Concrete improvements (≥3)

1. **Per-day artifact previews = real visual proof, zero asset dependency.** The 5-day section's biggest credibility gap was that the cards showed nothing tangible (number placeholders) while waiting on `day-1…5.png`. Each card now renders a recognizable mini-mockup of its deliverable in pure CSS — a wireframe for the landing-page day, palette + monogram for the brand day, a funnel diagram for the funnel day, etc. This delivers the SKILL.md "What you'll have on Day 5 visual proof" goal *now*, decoupled from the parallel visual-generation task. When real PNGs arrive they simply layer on top (the `<img>` covers the preview; on error it drops to opacity 0 and the preview shows) — no rework needed.
2. **CTA lever rotation → "Start in 5 Days".** The hero-pill rotation pool was exhausted last run, so this run rotates the other headline lever. "Start in 5 Days" is a concrete-outcome CTA (names the timeframe and the payoff) vs. the prior identity-framed "Build My System". Applied to both the hero primary and the mobile sticky button so the page tells one consistent story.
3. **Scrollytelling rail now works on mobile.** The scroll-linked progress rail was desktop-only. It's now visible on every breakpoint, giving mobile visitors the same felt "5-step sequence advancing as you scroll" cue that previously only desktop got — directly addressing the SKILL.md "vertical/mobile reveal" goal with the lowest-risk version (reuse the existing scroll math; no new listeners; still `prefers-reduced-motion` safe via the existing rule covering `.five-day-rail-fill` and `.five-day-dot`).

---

## 3. SKILL.md instructions — followed vs. skipped

**Followed:**
- *Build a "What you'll have on Day 5" visual proof section* — delivered as in-card CSS artifact previews (the credibility move, sourced without the missing mockups).
- *Test CTA copy* — rotated to `Start in 5 Days · $129` (hero + sticky). Rotation logged in §6.
- *Add scrollytelling for the 5-day journey (mobile)* — rail now renders on mobile; `prefers-reduced-motion` respected.
- *Polish: day-card image loading* — the `onerror` opacity-drop still governs real images; the fallback they reveal is now a substantive preview rather than a placeholder.
- *Date label rule* — log + footer use **month + year only** (`Jun 2026`); no weekday/day-of-month introduced.
- *Accountability log* + *passing build before exit* — this file; build green (§4).

**Skipped (with reason):**
- *Test hero pill copy* — held this run. The pill alternates are exhausted (currently the live `SUMMER SALE · $103 WITH SUMMER20` seasonal variant); rotated the CTA lever instead to keep the change isolated to one lever.
- *Avatar selector / Typography + Palette specimens / Testimonials placeholder* — already shipped in prior runs; left intact. No real buyer quotes exist yet, and faking them is explicitly rejected.
- *Solo-operator footer photo* — needs a real founder photo from the user; not inventable. The "why I built this" line is already present.
- *Re-enabling legacy 60-Minute offer* — left dormant (`{#if false}`) per "do not delete without explicit instruction."
- *Note on code:* page is internally consistent on the `SUMMER20` / Summer-sale framing (not the SKILL.md `VOS20` launch-week wording). Since it's June and every surface agrees, I treated it as an intentional seasonal swap and did **not** revert it.

---

## 4. Build result — `npm run build` (last ~18 lines)

First attempt hit the recurring **stale-artifact cleanup EPERM** (a locked leftover `.svelte-kit/output/client/.vite/manifest.json` on the mounted folder — not a code error). Resolved by moving the locked `.svelte-kit` aside; rebuilt clean:

```
.svelte-kit/output/server/_app/immutable/assets/_layout.mqmO8dK9.css   25.92 kB
.svelte-kit/output/server/entries/pages/_layout.js                      0.05 kB
.svelte-kit/output/server/chunks/ssr2.js                                0.10 kB
.svelte-kit/output/server/entries/pages/_layout.svelte.js               0.24 kB
.svelte-kit/output/server/internal.js                                   0.32 kB
.svelte-kit/output/server/chunks/server.js                              0.62 kB
.svelte-kit/output/server/chunks/utils2.js                              1.09 kB
.svelte-kit/output/server/entries/fallbacks/error.svelte.js             1.40 kB
.svelte-kit/output/server/chunks/ssr.js                                 3.89 kB
.svelte-kit/output/server/chunks/exports.js                             5.54 kB
.svelte-kit/output/server/chunks/internal.js                            8.69 kB
.svelte-kit/output/server/chunks/utils.js                              32.88 kB
.svelte-kit/output/server/remote-entry.js                              37.72 kB
.svelte-kit/output/server/index.js                                    131.74 kB
.svelte-kit/output/server/entries/pages/_page.svelte.js               238.22 kB
✓ built in 3.27s
  ✔ done
```

**Render verification** — grep of prerendered `index.html` (hit counts):
```
Start in 5 Days     : 1   (CTA rotated — hero + sticky)
Build My System     : 0   (old CTA fully removed)
day-fallback        : 1   (per-day artifact previews present)
five-day-rail block : 1   (rail now mobile-visible, no `hidden lg:` gate)
Checkout ✓          : 1   (Day-05 funnel preview rendered)
SUMMER20            : 1   (seasonal offer code intact)
```

---

## 5. Placeholder flags (NOT filled — for the user)

- **`src/routes/+page.svelte` GUMROAD const (lines ~47–49):** `[YOUR-GUMROAD-USERNAME]`, `[BUNDLE-SLUG]`, `[CORE-SLUG]`, `[UPGRADE-SLUG]` — all inside the **dormant legacy offer** (Bundle/Core/Upgrade). The active `launchpad` URL is real (`vanguardos.gumroad.com/l/launchpad`). Only matters if the legacy 60-Minute offer is ever re-enabled.
- **Missing image assets:** `static/products/day-1.png … day-5.png` still absent. As of this run the cards no longer *need* them to look finished — they show designed CSS artifact previews — but real screenshots would still upgrade the section. Pending the visual-generation task.
- **Locked stale build dirs:** `.svelte-kit-old-*`, `.svelte-kit-stale-*`, `.svelte-kit.bak*` could not be deleted (filesystem EPERM). Harmless to the build, but they accumulate; the user may want to clear them manually from Finder.
- No bracketed placeholders in active page copy.

---

## 6. Rotation / A-B state (for next runs)

- **Hero CTA:** now `Start in 5 Days · $129` (live, switched this run; hero + sticky). Tried so far: `Build My System` (prior), `Start in 5 Days` (now). Untried: `Launchpad Me`, control `Get The Launchpad — $129`.
- **Hero pill:** `SUMMER SALE · $103 WITH SUMMER20` (live, unchanged — seasonal). SKILL.md alternates exhausted.
- No analytics wired — rotations remain sequential qualitative swaps, not measured tests.

---

## 7. Recommendations for next run

1. **Swap real `day-N.png` mockups in** the moment they land — the preview fallbacks and `day-img` hooks are ready to receive them; nothing else changes.
2. **Rotate the CTA again** (`Launchpad Me` or the control) next cycle to keep the lever moving.
3. **Wire lightweight analytics** so the CTA/pill rotations become measured tests instead of guesses.
4. **Solo-operator footer photo** — drop in a real founder photo beside the existing "why I built this" note once supplied.
5. **Reconcile the offer code** — confirm with the user whether `SUMMER20` (live everywhere) or `VOS20` (SKILL.md) is the intended code, so source and spec agree.
