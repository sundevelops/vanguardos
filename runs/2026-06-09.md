# VanguardOS landing — iteration run · Jun 2026

Scheduled autonomous run. Focus: ship the recommended **scrollytelling for the 5-day journey**, rotate the last untried hero pill, and polish the day-card hover feel. Build verified green.

---

## 1. Files changed

### `src/routes/+page.svelte`
- **Hero pill (line ~503):** `★ NEW · THE OPERATOR'S LAUNCHPAD` → `LAUNCH WEEK · $103 WITH VOS20`. This was the last untried eyebrow variant from the SKILL.md rotation list; the rotation pool is now exhausted (next run should cycle back to the start or rotate the CTA instead).
- **Script state (line ~11):** added `let fiveDayProgress = 0;` — scroll-linked 0→1 progress value driving the new 5-day rail.
- **Scroll handler (`onScroll`):** added a block that reads `#five-day`'s `getBoundingClientRect()` each scroll tick and computes `fiveDayProgress` as the section travels through the viewport (fills from when the section top crosses 82% vh, completes near the section's exit). Reuses the existing passive scroll listener — no new listeners.
- **5-day section markup (`#five-day`):** added a desktop-only scrollytelling rail (`.five-day-rail`) above the day grid — a thin gold track whose fill scales with `fiveDayProgress`, plus 5 marker dots that light in sequence as progress passes each one. Each day card now carries a staggered `transition-delay` (`idx * 90ms`) so the cards cascade left-to-right on reveal instead of popping in together. Added `day-img` / `day-kicker` hooks to the card image and Day-N label.
- **`<style>` block:** new CSS for `.five-day-rail`, `.five-day-rail-track`, `.five-day-rail-fill`, `.five-day-rail-dots`, `.five-day-dot` / `.five-day-dot.is-lit`; plus day-card hover polish (`.day-img` 1.06 scale on hover, `.day-kicker` gold-bloom text-shadow on hover). Extended the `prefers-reduced-motion` block to disable all of the above (transitions off, hover transforms/text-shadow neutralized).

### `runs/2026-06-09.md`
- This log (new).

### Housekeeping
- Removed ~30 stale `vite.config.js.timestamp-*.mjs` scratch files and a stale locked `.svelte-kit` output dir that was causing an EPERM on build cleanup (see §4). No source impact.

---

## 2. Concrete improvements (≥3)

1. **Scrollytelling progress rail for the 5-day journey** — the day cards were static. There's now a scroll-linked gold rail with 5 sequentially-lighting marker dots directly above the row, plus the cards themselves cascade in left-to-right (staggered `transition-delay`). This turns the most important "what you actually get" section into a felt, progressive reveal — it visually reinforces the 5-day *sequence* (the core product promise) instead of presenting it as a flat grid. Pure scroll math + CSS, no animation library dependency on the rail, and fully `prefers-reduced-motion` safe.
2. **Day-card hover depth (image zoom + label bloom)** — day cards previously only lifted on hover. Now the card image scales to 1.06 and the "Day 0N" label warms to bright gold with a soft glow, matching the tactile, premium hover feel of the offer-stack cards. Makes the section feel interactive and worth exploring rather than decorative.
3. **Hero pill rotation → `LAUNCH WEEK · $103 WITH VOS20`** — swaps the eyebrow to a price-anchored urgency message during launch week. Surfaces the discount + code above the fold where it can pull the most weight, vs. the prior brand-flavor variant.
4. **Repo hygiene** — cleared dozens of stale `vite.config.js.timestamp-*.mjs` files and a locked stale build dir, removing the recurring EPERM build-cleanup failure mode for future runs.

---

## 3. SKILL.md instructions — followed vs. skipped

**Followed:**
- *Add scrollytelling for the 5-day journey* — implemented (desktop rail + staggered card reveal; Motion One not needed for the rail, kept animation budget low). `prefers-reduced-motion` respected.
- *Test hero pill copy* — rotated to the last untried alternate (`LAUNCH WEEK · $103 WITH VOS20`); rotation logged in §6.
- *Polish: day-card hover-states* — image zoom + gold label bloom added.
- *Date label rule* — log + footer use **month + year only** (`Jun 2026`); no weekday/day-of-month introduced anywhere.
- *Accountability log* — this file.
- *Passing build before exit* — verified (§4).

**Skipped (with reason):**
- *Test CTA copy* — held. SKILL.md treats pill and CTA as headline levers; rotated **one lever** (pill) this run to keep the change isolated. CTA stays `Build My System — $129`.
- *"What you'll have on Day 5" mockup proof row* & *day-card image loading* — `static/products/day-1…5.png` still absent (see §5), so the cards keep their branded fallback tiles. Building the proof row in earnest waits on the visual-generation task; the fallback already reads cleanly.
- *Avatar selector expansion / Typography + Palette specimens / Testimonials* — already shipped in prior runs; left intact, nothing actionable without new assets or real buyer quotes (faking testimonials explicitly rejected).
- *Solo-operator footer photo* — needs a real founder photo from the user; not inventable.
- *Re-enabling legacy 60-Minute offer* — left dormant (`{#if false}`) per "do not delete without explicit instruction."

---

## 4. Build result — `npm run build` (last ~18 lines)

First attempt failed on a **stale-artifact cleanup error** (not a code error): `EPERM: operation not permitted, unlink '.svelte-kit/output/client/.vite/manifest.json'` — a locked leftover output dir on the mounted folder. Resolved by moving the locked `.svelte-kit` aside so the build wrote a fresh one. Re-ran clean:

```
.svelte-kit/output/server/_app/immutable/assets/_layout.DTLHvAQ2.css   23.24 kB
.svelte-kit/output/server/_app/immutable/assets/_page.C2AkrYzY.css     25.69 kB
.svelte-kit/output/server/entries/pages/_layout.js                      0.05 kB
.svelte-kit/output/server/chunks/ssr2.js                                0.10 kB
.svelte-kit/output/server/entries/pages/_layout.svelte.js               0.24 kB
.svelte-kit/output/server/internal.js                                   0.32 kB
.svelte-kit/output/server/chunks/server.js                              0.62 kB
.svelte-kit/output/server/chunks/utils2.js                              1.09 kB
.svelte-kit/output/server/entries/fallbacks/error.svelte.js             1.40 kB
.svelte-kit/output/server/chunks/ssr.js                                 3.89 kB
.svelte-kit/output/server/chunks/exports.js                             5.54 kB
.svelte-kit/output/server/chunks/internal.js                            8.64 kB
.svelte-kit/output/server/chunks/utils.js                              32.88 kB
.svelte-kit/output/server/remote-entry.js                              37.72 kB
.svelte-kit/output/server/index.js                                    131.74 kB
.svelte-kit/output/server/entries/pages/_page.svelte.js               220.71 kB
✓ built in 3.18s
  ✔ done
```

**Render verification** — grep of prerendered `index.html` (hit counts):
```
LAUNCH WEEK · $103   : 1   (hero pill rotated)
five-day-rail        : 1   (scrollytelling rail present)
five-day-dot         : 1   (rail marker dots)
day-img              : 1   (hover-zoom image hook)
day-kicker           : 1   (hover-bloom label hook)
transition-delay     : 1   (staggered card cascade)
```

---

## 5. Placeholder flags (NOT filled — for the user)

- **`src/routes/+page.svelte` GUMROAD const (lines ~45–49):** `[YOUR-GUMROAD-USERNAME]`, `[BUNDLE-SLUG]`, `[CORE-SLUG]`, `[UPGRADE-SLUG]` — **all inside the dormant legacy offer** (Bundle/Core/Upgrade). The active `launchpad` URL is real (`vanguardos.gumroad.com/l/launchpad`). Only matters if the legacy 60-Minute offer is ever re-enabled.
- **Missing image assets:** `static/products/day-1.png … day-5.png` still absent. The 5-day cards degrade to designed branded fallback tiles (no broken images). Real mockups still pending the visual-generation task.
- No other fill-in placeholders in active page copy. (Bracketed matches like `[0]`/`[90]` are array indices and z-index utility classes, not placeholders.)

---

## 6. Rotation / A-B state (for next runs)

- **Hero pill:** now `LAUNCH WEEK · $103 WITH VOS20` (live, switched this run). **All three SKILL.md alternates are now exhausted** (`IDEA TO LIVE IN 5 DAYS` → `THE OPERATOR'S LAUNCHPAD` → this). Next run: either rotate the **CTA** lever instead, or cycle the pill back to a prior winner.
- **Hero CTA:** `Build My System — $129` (live, unchanged). Untried: `Launchpad Me`, `Start in 5 Days`, control `Get The Launchpad — $129`.
- No analytics wired — these remain sequential qualitative swaps, not measured tests.

---

## 7. Recommendations for next run

1. **Rotate the CTA lever** (`Start in 5 Days` is a strong concrete-outcome candidate) since the pill pool is exhausted.
2. **Day-5 mockup proof row** — build the moment `day-1…5.png` land from the visual task; fallback tiles + image hooks (`day-img`) are already wired to receive them.
3. **Mobile scrollytelling** — the new rail is desktop-only (cards are a single row at `lg`). Consider a vertical pin-and-reveal variant for the stacked mobile layout if engagement on small screens matters.
4. **Solo-operator footer photo** — drop in a real founder photo beside the existing "why I built this" note once supplied.
5. **Wire lightweight analytics** so pill/CTA rotations become measured tests instead of qualitative guesses.
